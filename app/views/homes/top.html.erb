<%= flash[:notice] %>
<br />
<br />

<div class="container">
    <div class="col-md-auto">
      <div class="text1">
        ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。<br>
      </div>
    </div>
</div>



<div class="container">
  <h3>新着商品</h3>
    <div class="row">
      <% @items.each do |item| %>
      <div class="col-md-3 px-3">
        <div class="image">
        <!--each分は単数形のみには使用しない-->
          <% if item.image.attached? %>
            <%= image_tag item.image, size: "400x300", class: "item-image" %>
          <% else %>
            <%= image_tag 'noimage', size: "400x300", class: "item-image" %>
          <% end %>
        </div>
        <%= item.name %><br />
        <%= item.price %>
      </div>
      <% end %>
    </div>
    <p class="text-right">
      <%= link_to "全ての商品を見る > ", public_items_path(current_customer) %>
　　</p>
</div>

