
<h2>マイページ</h2>

<h3>登録情報</h3><%= link_to "編集する", edit_public_customer_path(current_customer), class: "btn btn-success" %>


<table class="table">
  <tbody>
    <tr>
      <td>氏名</td>
      <td><%= current_customer.last_name %>　<%= current_customer.first_name %></td>
    </tr>
    <tr>
      <td>カナ</td>
      <td><%= current_customer.last_name_kana %>　<%= current_customer.first_name_kana %></td>
    </tr>
    <tr>
      <td>郵便番号</td><td><%= current_customer.postal_code %></td>
    </tr>
    <tr>
      <td>住所</td><td><%= current_customer.address %></td>
    </tr>
    <tr>
      <td>電話番号</td><td><%= current_customer.telephone_number %></td>
    </tr>
    <tr>
      <td>メールアドレス</td><td><%= current_customer.email %></td>
    </tr>
  </tbody>
</table>

以下パス変える<br /><br />

配送先<%= link_to "一覧を見る", edit_public_customer_path(current_customer), class: "btn btn-primary" %><br /><br />
注文履歴<%= link_to "一覧を見る", edit_public_customer_path(current_customer), class: "btn btn-primary" %><br />