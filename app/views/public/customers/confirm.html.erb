<div class="container">
  <div class="row">
    <div class="col">
      <h2 class="text-center">
        本当に退会しますか？
        </h2>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col">
      <p class="text-center">
        退会すると、会員登録情報や<br />
        これまでの購入履歴が閲覧できなくなります。<br />
        退会する場合は、「退会する」をクリックしてください。<br />
      </p>
    </div>
  </div>

  <div class="row justify-content-center">
    <div class="btn-group">
    　<!--< % if customer_signed_in? %>-->
      <!--< % end %>-->
      <button class="btn"><%= link_to "　退会しない　", customers_mypage_path(@customer), class: "btn btn-primary" %></button>
      <button class="btn"><%= link_to "　 退会する 　", withdraw_path(@customer), method: :patch, class: "btn btn-danger" %></button>
    </div>
  </div>
</dic>